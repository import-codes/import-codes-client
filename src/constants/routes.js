/**
 * Routes definitions file
 */
const routes = {
    root: { path: '/' },
    login: { path: '/login' },
    dashboard: { path: '/dashboard' },

    error404: { path: '/404' }
}

export default routes;
